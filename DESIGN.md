# Design System — MURAD · Digital Product Studio

> المرجع الأساسي الوحيد. كل عنصر في الموقع يجب أن يخرج من هذا النظام. لا قيمة عشوائية، لا لون خارج التوكنز، لا خط خارج المقياس، لا مسافة خارج السلّم.
> الإحساس المستهدف: **دليل هوية بصرية عالمي (Brand Book)** — Editorial، فاخر، مدروس، بمستوى Awwwards، وبهوية أصلية مبنية على مشاريع مراد الحقيقية.

---

## 0 · Design Principles

1. **الطباعة هي البطل** — العناوين ضخمة، Editorial، عالية التباين. التصميم يُقاد بالحجم والتباين والفراغ قبل اللون.
2. **كل صفحة قطعة فنية (Poster)** — تكوين، طبقات، عُمق، Editorial Layout، إيقاع متغيّر. لكل قسم شخصية داخل نفس النظام.
3. **الفراغ عنصر تصميم** — المساحات البيضاء محسوبة ومقصودة، ليست فراغًا بل توتّر بصري.
4. **التفاصيل تصنع الفرق** — خطوط رفيعة، إحداثيات، أرقام أقسام، تسميات، قياسات، ميكرو-تايبوغرافي. إحساس دليل هوية.
5. **هوية واحدة بلغتين** — العربي والإنجليزي صُمِّما معًا كندّين، لا ترجمة.
6. **الانضباط اللوني** — بالِت صغيرة، كل لون بوظيفة واحدة. لا لون جديد بلا سبب نظامي.
7. **الحركة راقية وهادفة** — سينمائية، بطيئة، ناعمة. لا حركة استعراضية؛ كل انتقال له سبب.
8. **كل شيء حقيقي** — لا Placeholder ولا Mockup عام. كل صورة/قصاصة من مشاريع مراد وإنجازاته الفعلية.

**اختبار السلوب (Impeccable):** لو أمكن قول "AI سوّاها" → فشل. المعيار: أن يسأل الزائر "كيف بُني هذا؟". يُفحص كل تعديل بـ `npx impeccable detect` (الهدف `[]`).

---

## 1 · Color System (Tokens)

بالِت مقتصدة عمدًا. القيم مرجعية HEX + OKLCH.

| Token | HEX | OKLCH | الدور (وظيفة واحدة) |
|---|---|---|---|
| `--charcoal` | `#1B1815` | `oklch(0.24 0.008 60)` | الحبر: العناوين، النص، القيادة، الحدود القوية |
| `--espresso` | `#3C2E26` | `oklch(0.33 0.022 55)` | العُمق: طبقات ثانوية، خلفيات داكنة بديلة |
| `--ivory` | `#F2ECE0` | `oklch(0.94 0.014 85)` | الخلفية الأساسية (Warm Ivory — خيار مُلتزم مقصود) |
| `--paper` | `#F8F4EA` | `oklch(0.96 0.012 85)` | سطح مرتفع: بطاقات، لوحات، قصاصات |
| `--dust` | `#96A9B6` | `oklch(0.70 0.030 235)` | توازن بارد نادر: تمييز، حالات، رسوم |
| `--gold` | `#B49468` | `oklch(0.67 0.055 75)` | تأكيد خافت جدًا: خطوط، علامات، أرقام — **لا يُستخدم لنص أساسي** |
| `--hair` | `#D9CFBE` | `oklch(0.85 0.016 82)` | حدود وخطوط شبكة رفيعة |
| `--muted` | `#6B6154` | `oklch(0.46 0.017 70)` | نص ثانوي/تسميات (≥4.5:1 على ivory) |

**قواعد التباين (WCAG AA):**
- نص أساسي = `--charcoal` على `--ivory` → تباين عالٍ جدًا ✅.
- نص ثانوي = `--muted` على `--ivory` → ≥ 4.5:1 ✅. لا رمادي أفتح للنص.
- `--gold` تباينه منخفض على ivory → للزينة/الخطوط/العلامات فقط، ممنوع كنص.
- الوضع الداكن (لأقسام Poster): خلفية `--charcoal`/`--espresso` + نص `--ivory`.

**استراتيجية اللون:** Restrained (محايدات دافئة + ذهب ≤ 5% كتأكيد). اللون لا يصرخ؛ الطباعة والفراغ يقودان.

---

## 2 · Typography System

الخطوط (كلها خارج قائمة Impeccable المرفوضة):

| الدور | الخط | الاستخدام |
|---|---|---|
| **Display (EN)** | `Bodoni Moda` (Didone عالي التباين) | العناوين الضخمة، كلمة تملأ الشاشة، أرقام كبيرة |
| **Text/UI (EN)** | `Archivo` (Grotesque) | نص، أزرار، تسميات، تنقّل |
| **Micro (EN)** | `JetBrains Mono` | إحداثيات، captions، قياسات، أرقام أقسام، labels تقنية |
| **Display (AR)** | `Aref Ruqaa` | العناوين العربية الكبيرة، اللحظات الفاخرة |
| **Text (AR)** | `Amiri` | النص العربي الطويل، الاقتباسات |

> ملاحظة: العربي والإنجليزي يُضبطان بصريًا (حجم/وزن/تتبّع) ليتوازنا على نفس السطر — العربي ليس أصغر افتراضيًا.

### Type Scale (fluid, نسبة ≥ 1.25)
```
--t-giant : clamp(4.5rem, 20vw, 15rem)   /* اسم/كلمة واحدة تملأ الشاشة */
--t-d1    : clamp(3rem, 9vw, 7rem)        /* عناوين أقسام بطولية */
--t-d2    : clamp(2.25rem, 5.5vw, 4rem)   /* عناوين فرعية Editorial */
--t-h3    : clamp(1.6rem, 3vw, 2.4rem)
--t-lead  : clamp(1.125rem, 2.2vw, 1.4rem)/* جُمل تمهيدية */
--t-body  : 1.0625rem                       /* نص */
--t-small : 0.9rem
--t-micro : 0.72rem                          /* mono labels، letter-spacing .12–.28em */
```
- Display: `letter-spacing: -0.02em`، `line-height: .85–.92`، `text-wrap: balance`.
- Body: `line-height: 1.8`، عرض السطر ≤ 68ch، `text-wrap: pretty`.
- Micro/labels: uppercase + `letter-spacing: .18–.28em`.
- **التباين بين الأحجام حاد جدًا** (giant بجانب micro) — جزء من الهوية.

---

## 3 · Grid System

- **12 عمودًا**، حاوية قصوى `1280px`، هوامش `clamp(24px, 6vw, 96px)`.
- **Gutter:** `24px` (ديسكتوب) / `16px` (موبايل).
- **Baseline:** شبكة `8px` لكل الإيقاع الرأسي.
- **خطوط الشبكة مرئية أحيانًا** كعنصر Art Direction (hairlines بلون `--hair`).
- **كسر الشبكة مقصود:** عناصر تخرج عن الأعمدة (full-bleed، overlap، عناصر مزاحة) للتأكيد — لكن ضمن النظام لا عشوائيًا.
- **إطار الصفحة:** حدّ رفيع `--hair` بزوايا مربّعة صغيرة (corner marks) بإحساس دليل الهوية (اختياري لكل قسم).

---

## 4 · Spacing Scale (8px base)

```
--s-1: 4px    --s-2: 8px    --s-3: 12px   --s-4: 16px
--s-5: 24px   --s-6: 32px   --s-7: 48px   --s-8: 64px
--s-9: 96px   --s-10: 128px --s-11: 160px --s-12: 200px
```
- **إيقاع متغيّر:** تجميعات ضيّقة (عنوان+تسمية = `--s-2`) مقابل فواصل واسعة بين الأقسام (`--s-10`/`--s-11`).
- Padding الأقسام الرأسي: `clamp(80px, 14vh, 200px)`.

---

## 5 · Layout Rules

- كل قسم = **Poster** مستقل: تكوين، طبقات، توازن غير متماثل عند الحاجة.
- **Asymmetry متعمّد**: لا تمركز افتراضي؛ ثقل بصري موزّع بقصد.
- **Overlap & Depth**: عناصر متداخلة بظلال ناعمة جدًا وطبقات z واضحة.
- **Full-bleed** للصور البطولية؛ نص محاذى للشبكة فوقها.
- **تنوّع الإيقاع** بين الأقسام: قسم هادئ واسع ← قسم كثيف بالمعلومة ← قسم بصري بحت.
- **أرقام الأقسام** (01–07) وإحداثيات وتسميات على الحواف كلغة مستمرة.

---

## 6 · Iconography

- أيقونات **خط رفيع (1.25–1.5px)**، هندسية دقيقة، بنفس رُقيّ الطباعة.
- مجموعة مخصّصة صغيرة (سهم، رابط خارجي، +، إحداثية، علامة قسم) — لا مكتبة أيقونات عامة ملوّنة.
- الأيقونة بلون `--charcoal` أو `--gold` فقط.

---

## 7 · Patterns (اللغة البصرية المتكرّرة)

- **Grid lines / hairlines** خلفية خفيفة.
- **Coordinates** (`N 21.54° · E 39.17°`) و`X/Y` markers.
- **Section markers** (`01 — 07`) بخط Display صغير + label.
- **Measurement ticks** (خطوط قياس بنهايات صغيرة) حول العناصر.
- **Captions/labels** mono على القصاصات (`FIG. 03.1`, `LANDING / STREET-56.tsx`).
- **Corner marks** بزوايا الإطار.
- **Gold hairline** كفاصل نادر فاخر.
- **Collage/Moodboard**: قصاصات حقيقية متداخلة بزوايا دوران خفيفة (−4° إلى +4°) وحدود دقيقة.

---

## 8 · Art Direction

- **Hero = Moodboard حيّ** من **عناصر مراد الحقيقية**: Screenshots مواقعه، UI صمّمها، Dashboards، Mobile screens، Design systems، Wireframes، Figma frames، Components، Git commits، Code snippets، Flow charts، Notes، Sketches — مرتّبة فنيًا (Collage/Overlap/Layers) لا عشوائيًا. أول انطباع: **استوديو يصنع منتجات رقمية عالمية**.
- **لكل مشروع عالمه**: خلفية/طباعة/تكوين خاص، وكأنه Brand مستقل داخل الموقع.
- **Storytelling بصري**: القصة تُروى بالتصميم قبل النص.
- **مصدر الصور الوحيد = أعمال مراد**. ممنوع أي stock أو placeholder عام في النسخة النهائية.

---

## 9 · Image Treatment

- الصور داخل **إطار دقيق** (`1px --charcoal` أو `--hair`) مع **caption mono** أعلى/أسفل.
- معالجة موحّدة اختيارية: تدفئة خفيفة/تقليل تشبّع بسيط ليتناغم مع البالِت الدافئة (بلا فلتر ثقيل).
- **أحجام ضخمة** للصور البطولية (full-bleed أو ≥ 60vw).
- **Overlap** مقصود مع نص/عناصر، بظل ناعم جدًا (`0 24px 60px -30px #1B181540`).
- نِسب مقصودة (3:2، 4:5 للموبايل، 16:9 للشاشات) — لا تشويه.
- `alt` وصفي جزء من الصوت.

---

## 10 · Components

### Buttons
- **Primary:** خلفية `--charcoal`، نص `--ivory`، بلا حواف دائرية (حادّ) أو نصف قطر `2px` كحد أقصى، Padding `14px 26px`، Archivo 600. Hover: رفع `−2px` + تغميق طفيف (200ms).
- **Ghost:** حد `1.5px --charcoal`، نص `--charcoal`. Hover: تعبئة `--charcoal`/نص `--ivory`.
- **Link/Text:** نص + خط سفلي رفيع يتمدّد عند hover (سهم `↗`).
- حالات focus مرئية (خط `--gold` 2px).

### Cards
- **قاعدة عامة:** البطاقات ليست الحل الافتراضي، وممنوعة تمامًا لعرض المشاريع.
- عند اللزوم (مثلاً قائمة مهارات/خدمات): سطح `--paper`، حد `1px --hair`، بلا تكديس بطاقات داخل بطاقات، بلا side-stripe borders.

### Navigation
- شريط علوي شفاف → يكتسب خلفية `--ivory` + hairline سفلي عند التمرير.
- شعار نصّي `MURAD` (Bodoni) + نقطة `--gold`.
- مبدّل لغة `ع / EN` واضح (يبدّل الاتجاه RTL/LTR فعليًا).
- روابط Archivo، تباعد واسع، مؤشر تفاعل رفيع.
- نسخة موبايل: قائمة كاملة الشاشة Editorial (عناوين كبيرة، أرقام).

### Forms (Contact)
- حقول بخط سفلي فقط (underline field) بلون `--hair` → `--charcoal` عند focus، بلا صناديق ثقيلة.
- Labels mono صغيرة فوق الحقل.
- زر إرسال Primary. حالات: تحميل، نجاح، خطأ — نصوص واضحة بالعربي/EN.
- زر **واتساب** بارز كبديل مباشر.

### القوائم/الإحصاءات
- بلا "hero-metric template". الأرقام تُعرض بخط Display كبير مع label mono، ضمن تكوين لا كشبكة أرقام مكرّرة.

---

## 11 · Case Study Template (لكل مشروع)

كل مشروع صفحة/قسم مستقل بشخصيته:
1. **Cover:** اسم المشروع بخط Display ضخم + سنة + تصنيف + صورة بطولية حقيقية.
2. **Intro:** الفكرة + دوري (Role) + التقنيات (mono tags).
3. **Challenge — المشكلة.**
4. **Process:** عملية التصميم/التطوير (wireframes، تكرارات، لقطات حقيقية، ملاحظات).
5. **Solution:** الحل بصور ضخمة + تفاصيل UI.
6. **Results:** نتائج/أثر (أرقام إن وُجدت).
7. **Next project** انتقال أنيق.
- خلفية/لكنة لونية خاصة لكل مشروع (ضمن النظام)، لكن نفس لغة التفاصيل (captions/coordinates/measurements).

---

## 12 · Motion System

- **المكتبات:** `Lenis` (smooth scroll)، `GSAP + ScrollTrigger` (تسلسل سينمائي)، `Framer Motion` (مكوّنات React).
- **المبدأ:** بطيء، ناعم، سينمائي، هادف. لا bounce/elastic.
- **التوكنز:**
  ```
  --e-out: cubic-bezier(0.16, 1, 0.3, 1)   /* ease-out-expo */
  --e-inout: cubic-bezier(0.65, 0, 0.35, 1)
  --d-fast: 240ms   --d-base: 520ms   --d-slow: 900ms   --d-cine: 1400ms
  ```
- **Text reveal:** كشف الأسطر بقناع (mask) صعودًا، stagger `60–90ms`.
- **Image reveal:** كشف بقناع/clip + scale خفيف `1.06→1` (`--d-cine`).
- **Parallax:** لطيف (`≤ 12%`) على الصور/القصاصات.
- **Hover:** رفع ناعم + blur/scale خفيف على الصور.
- **Page transitions:** fade/clip سينمائي بين الحالات.
- **إلزامي:** `@media (prefers-reduced-motion: reduce)` → خفوت فوري/إلغاء parallax. المحتوى مرئي افتراضيًا (الكشف يحسّن لا يخفي).

---

## 13 · Interaction Patterns

- **Cursor:** مؤشر مخصّص دقيق (نقطة/دائرة) يتضخّم/يتحوّل عند العناصر التفاعلية (magnetic) — راقٍ لا لعبة.
- **Scroll progress:** مؤشر تقدّم رفيع أو أرقام أقسام تتحدّث.
- **Reveal on scroll:** فوق حالة مرئية، لا يخفي المحتوى.
- **Hover على المشاريع:** إبراز + معاينة + سهم.
- **Focus states** واضحة لكل عنصر تفاعلي (لوحة مفاتيح).

---

## 14 · Responsive Rules

- **Breakpoints:** `sm 480 · md 768 · lg 1024 · xl 1280 · 2xl 1536`.
- **Mobile-first**، ثم توسيع.
- العناوين الضخمة تُختبر عند كل breakpoint — **ممنوع تجاوز النص لحاويته**؛ يُقلّل `clamp` أو تُعاد صياغة النص.
- الموبايل: تكوينات Poster تتبسّط لكن تحتفظ بالتفاصيل (captions/coordinates أصغر).
- الأصابع: أهداف لمس ≥ 44px.
- RTL/LTR كامل بالانعكاس المنطقي (`inset-inline`, `margin-inline`).

---

## 15 · Accessibility

- WCAG **2.1 AA** كحد أدنى.
- تباين نص أساسي ≥ 4.5:1، كبير ≥ 3:1 (متحقّق في §1).
- `prefers-reduced-motion` مدعوم بالكامل.
- تنقّل لوحة مفاتيح + focus مرئي + ترتيب منطقي + skip link.
- HTML دلالي، ARIA عند الحاجة، `lang`/`dir` صحيحان لكل لغة.
- صور بنص بديل وصفي؛ الحركة لا تُخفي محتوى.

---

## 16 · Tech & Build (لاحقًا)

- **Next.js (App Router) + TypeScript + Tailwind** (توكنز النظام كـ CSS variables + Tailwind theme).
- `next-intl` للعربي/الإنجليزي + RTL أصيل.
- `next/font` لتحميل الخطوط بأداء عالٍ.
- Lenis + GSAP + Framer Motion للحركة.
- نشر على **Vercel** بدومين باسم مراد.
- فحص كل واجهة بـ **Impeccable detect** قبل الاعتماد.

---

## 17 · Guardrails (ممنوعات — يفحصها Impeccable)

لا خطوط مرفوضة (Inter/Space Grotesk/Playfair...) · لا gradient-text · لا side-stripe borders · لا glassmorphism افتراضي · لا شبكة بطاقات متطابقة للمشاريع · لا eyebrows/أرقام أقسام كديكور بلا معنى · لا نيون · لا تدرّجات مبالغ · لا Placeholder/stock في النسخة النهائية · لا حركة استعراضية · لا لون خارج التوكنز.
